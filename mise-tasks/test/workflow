#!/usr/bin/env bash
set -euo pipefail
command -v "${PDFTOPPM_BIN:-pdftoppm}" >/dev/null || { echo "missing ${PDFTOPPM_BIN:-pdftoppm} (poppler-utils)"; exit 1; }
mise x node@24.13.1 -- node --test --test-concurrency=1 apps/api/test/workflow.integration.test.mjs
mkdir -p .cache
date +%s > .cache/test-workflow.stamp
