#!/usr/bin/env bash
set -euo pipefail

if [ "${1-}" = "" ]; then
  echo "usage: mise run dbos:workflow:steps -- <workflow-id>" >&2
  exit 2
fi

pnpm --filter @jejakekal/api exec dbos workflow steps -s "$DBOS_SYSTEM_DATABASE_URL" "$1"
