export const UI_PLANE_IDS = Object.freeze({
  conversation: 'conversation-plane',
  execution: 'execution-plane',
  artifact: 'artifact-plane'
});

export const UI_ALIAS_IDS = Object.freeze({
  conversation: 'conv',
  execution: 'exec',
  artifacts: 'artifacts'
});

export const UI_RUN_STATUS_STATES = Object.freeze(['idle', 'running', 'done', 'error']);
